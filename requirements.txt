# ================================================================
# Enterprise Balance Sheet Extraction Pipeline
# Requirements — Python 3.10+
# Install: pip install -r requirements.txt
# ================================================================

# ── PDF Processing ───────────────────────────────────────────────
pymupdf>=1.24.0              # PyMuPDF (fitz) — text extraction, page rendering, DPI control
pdfplumber>=0.11.0           # Cell matrix extraction — critical for column alignment fix

# ── Image Processing & OCR ───────────────────────────────────────
Pillow>=10.0.0               # Image handling (PNG conversion from PDF pages)
opencv-python-headless>=4.9.0  # Grid line detection for scanned PDFs (no GUI needed)
pytesseract>=0.3.10          # Python wrapper for Tesseract OCR engine
numpy>=1.26.0                # Required by OpenCV

# ── Row Mapping ──────────────────────────────────────────────────
rapidfuzz>=3.6.0             # Fuzzy label matching — canonical row assignment

# ── LLM Integration (Ollama local API) ───────────────────────────
requests>=2.31.0             # HTTP calls to Ollama API (http://localhost:11434)

# ── Excel Export ─────────────────────────────────────────────────
openpyxl>=3.1.2              # Excel write with cell colours, borders, formatting
pandas>=2.2.0                # DataFrame operations for tabular data

# ── Utilities ────────────────────────────────────────────────────
python-dotenv>=1.0.0         # Load .env for OLLAMA_BASE_URL, TESSERACT_CMD overrides

# ================================================================
# External binaries required (NOT pip-installable):
# ================================================================
#
# 1. Tesseract OCR 5.x
#    Windows : https://github.com/UB-Mannheim/tesseract/wiki
#              Install path: C:\Program Files\Tesseract-OCR\tesseract.exe
#              Language pack: English (eng.traineddata)
#    Linux   : sudo apt install tesseract-ocr tesseract-ocr-eng
#
# 2. Ollama (local LLM runtime)
#    Download: https://ollama.com/download
#    After install:
#      ollama serve                  # start server
#      ollama pull gemma3:4b         # Agent 1 — Extractor
#      ollama pull llama3.2:3b       # Agent 2 — Verifier
#
# ================================================================
# Optional — Deep learning table detection (heavy, use only if
# OpenCV grid detection fails on complex borderless tables):
# ================================================================
#
#   pip install transformers timm torch torchvision \
#       --index-url https://download.pytorch.org/whl/cpu
#
# ================================================================
